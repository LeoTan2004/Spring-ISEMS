---
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/monitor_system?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  servlet:
    session:
      cookie:
        http-only: false
mybatis-plus:
  global-config:
    db-config:
      table-underline: true
  configuration:
    map-underscore-to-camel-case: true